module.exports = {
  env: {
    embertest: true
  },
  globals: {
    // Specify globals with a value of `true` or `false`:
    // `false` will make it read-only
    // `true` will allow the variable to be reassigned
    authenticate: false,
    Viz: true,
  }
};
